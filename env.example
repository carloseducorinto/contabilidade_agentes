# =================================================================
# CONFIGURAÇÃO DE AMBIENTE - SISTEMA DE CONTABILIDADE COM AGENTES
# =================================================================

# --- OPENAI CONFIGURATION ---
# Chave da API OpenAI (OBRIGATÓRIA para processamento de imagens e fallback LLM)
OPENAI_API_KEY=sk-your-openai-api-key-here

# --- BACKEND CONFIGURATION ---
# Ambiente de execução
ENVIRONMENT=production

# Nível de log (DEBUG, INFO, WARNING, ERROR)
LOG_LEVEL=INFO

# Origens CORS permitidas (separadas por vírgula)
CORS_ORIGINS=http://localhost:8501,http://127.0.0.1:8501

# Configurações de processamento
MAX_FILE_SIZE_MB=50
MAX_CONCURRENT_PROCESSING=5
MAX_BATCH_SIZE=10

# Configurações LLM
LLM_MODEL=gpt-4o
LLM_TEMPERATURE=0.1
LLM_MAX_TOKENS=4000

# Configurações OCR
PDF_DPI=300
OCR_LANGUAGE=por
TESSERACT_PSM=6

# --- FRONTEND CONFIGURATION ---
# URL do backend (para Docker use http://backend:8000)
BACKEND_URL=http://backend:8000

# Configurações Streamlit
STREAMLIT_SERVER_HEADLESS=true
STREAMLIT_BROWSER_GATHER_USAGE_STATS=false

# --- MONITORING CONFIGURATION (Opcional) ---
# Senha do admin do Grafana
GRAFANA_PASSWORD=admin123

# --- SECURITY CONFIGURATION ---
# Chave secreta para JWT (gere uma chave aleatória)
SECRET_KEY=your-secret-key-here

# --- CACHE CONFIGURATION ---
ENABLE_CACHING=true
CACHE_TTL=3600

# --- DATABASE CONFIGURATION (Futuro) ---
# DATABASE_URL=postgresql://user:password@localhost:5432/contabilidade

# =================================================================
# INSTRUÇÕES:
# 1. Copie este arquivo para .env
# 2. Substitua os valores pelos seus valores reais
# 3. NUNCA commite o arquivo .env no Git!
# ================================================================= 